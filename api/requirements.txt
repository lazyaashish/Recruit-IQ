# RecruitIQ — Vercel serverless requirements
# NOTE: sentence-transformers / torch are intentionally excluded.
# The EmbeddingService has a built-in TF-IDF fallback that is used automatically
# when sentence-transformers is not importable. This keeps the lambda under
# Vercel's 250 MB limit. For GPU-accelerated embeddings, run self-hosted.
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
sqlalchemy==2.0.36
pydantic[email]==2.10.3
pydantic-settings==2.6.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
scikit-learn==1.5.2
numpy==1.26.4
pdfplumber==0.11.4
python-docx==1.1.2
structlog==24.4.0
